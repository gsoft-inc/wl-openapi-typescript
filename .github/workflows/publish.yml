name: Publish

on:
  push:
    branches:
    - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    permissions:
      id-token: write # required for provenance https://docs.npmjs.com/generating-provenance-statements#publishing-packages-with-provenance-via-github-actions
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - name: Pre LinearB
        id: linearb-pre
        uses: ./.github/actions/linearb-pre

      - name: Setup project
        uses: ./.github/actions/setup

      - name: Create Release Pull Request or Publish to npm
        id: changesets
        uses: changesets/action@v1
        with:
          publish: pnpm run changeset:publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.WORKLEAP_NPM_TOKEN }}
      
      - name: LinearB
        if: ${{ steps.changesets.outputs.published == 'true' }}
        uses: ./.github/actions/linearb-main
        with:
          environment: release
          timestamp: ${{ steps.linearb-pre.outputs.timestamp }}