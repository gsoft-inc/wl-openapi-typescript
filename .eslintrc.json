{
    "$schema": "https://json.schemastore.org/eslintrc",
    "root": true,
    "extends": ["plugin:@workleap/typescript-library"],
    "rules": {
        "jest/no-deprecated-functions": "off" // This rule doesn't work because we're not using jest
    }
}
